# Template web route

Minimal, dependency-free route artifact for verifying the Phase 1 web delivery
path. It is not the Phase 2 shell. Copy it only as a starting point and preserve
the visible generation marker and accessibility checks.
