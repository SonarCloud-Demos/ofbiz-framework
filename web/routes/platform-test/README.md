# Platform test route

This route proves shell composition, the modern marker, same-origin BFF access, error handling, and fallback. It owns no business data and is removed after the first production slice replaces it.
