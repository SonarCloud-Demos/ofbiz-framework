# Modern web shell

This is the Phase 2 same-origin shell. Its checked-in route manifest drives the
visible generation marker. The edge remains authoritative for the corresponding
response header and routing target.

The initial navigation intentionally links back to legacy OFBiz. Authentication
must be terminated by the future BFF before this shell is exposed to users.
