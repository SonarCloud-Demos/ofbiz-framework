# Edge module

Creates the Phase 1 APIM and Front Door Premium primitives. Phase 2 owns API
definitions, WAF policy, private origin approval, custom domains, route catalog,
identity/session behavior, and traffic cutover. Do not expose an origin until
those controls and the Phase 2 security tests are ready.
